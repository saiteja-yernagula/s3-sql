clauses 
where group by order by distinct limit offset 
having

aggregating functions
sum max min avg count 

multiple group by
select deptno ,job from emp group by deptno , job;

having
it is used after group by to apply a conditon with
aggregating functions
select deptno,sum(sal) from emp group by deptno having sum(sal)<10000;

get the deptno whose count having more than 1emp
select deptno,count(*) from emp group by deptno having  count(*)>1;

find the highest no of emp in a table based on a deptno
select deptno,count(*) from emp group by deptno 
 having  count(*)>1 order by count(*) desc limit 1;

group by with where clause
select job,sum(sal) from emp where job<>'clerk' 
         group by job ;


select * from emp where sal>max(sal);



single row functions
1)  upper
select upper(ename) from emp;

2) lower
select lower(ename) from emp;

3) length
select length(ename) from emp;

4) concat
select concat("mr ",ename,"-",job) from emp;

5) substr
select substr(ename, 1,3) from emp;

math 
ceil()
round()
floor()

select sal,ceil(sal),round(sal),floor(sal) from emp;

date functions
year()
month()
day()
select year(hiredate), month(hiredate),day(hiredate)
     from emp;

select now();
select hour(now());
select minute(now());
select second(now());
select datediff(curdate(),hiredate) from emp;
